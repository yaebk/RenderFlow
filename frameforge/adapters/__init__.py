"""Host adapters for specific applications.

The FrameForge core never imports anything from here - adapters depend on the
core, not the other way round. Import the one you need explicitly::

    from frameforge.adapters.resolve import ResolveHost
"""
